#!/usr/bin/ruby
require_relative "advent3"
raise "Usage: solve <puzzle-data>" if  ARGV.empty?
puts "Part1: #{Advent3.new(File.new(ARGV.first).read).part1}"
puts "Part2: #{Advent3.new(File.new(ARGV.first).read).part2}"
